import string
def unique_words():
        reserve = []
        f = open('Book1.txt','r')

        for line in f:
             res = line.lower().strip(string.punctuation + string.whitespace) 
             reserve += res
        return reserve
            if words >= 50:
              return found
            else
              return not found

print(unique_words())

    
 
     
